export interface Recipe {
  id: number;
  title: string;
  ingredients: string;
  instructions: string;
  imageUrl?: string;
  website_url?: string;
  image_url?: string;
  created_at: string;
  updated_at: string;
}
